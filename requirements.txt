# requirements.txt
#
# Este archivo especifica todas las dependencias necesarias para ejecutar el proyecto.
# Usar un archivo requirements.txt permite:
# - Instalar fácilmente todas las librerías requeridas con un solo comando (pip install -r requirements.txt)
# - Garantizar la compatibilidad y reproducibilidad del entorno de desarrollo y producción
# - Facilitar la colaboración entre desarrolladores y la integración continua

Flask==3.0.3           # Framework web para crear APIs y aplicaciones web
SQLAlchemy==2.0.30     # ORM para interactuar con bases de datos relacionales usando objetos Python
pymysql==1.1.0         # Driver para conectar SQLAlchemy con bases de datos MySQL
psycopg2-binary==2.9.9 # Driver PostgreSQL para SQLAlchemy (Railway compatible)
python-dotenv==1.0.1   # Cargar variables de entorno desde archivos .env
pandas                 # Lector para el csv
PyJWT==2.8.0          # JSON Web Tokens para autenticación (usado en auth_utils.py)
bcrypt==4.0.1         # Hash seguro de contraseñas
Flask-JWT-Extended==4.6.0  # Extensión de Flask para manejo simplificado de JWT
Werkzeug==3.0.3       # Librería para hashing de contraseñas (ya incluida con Flask)
gunicorn==21.2.0      # Servidor WSGI para producción
pytest==8.3.3         # Framework para testing
pytest-flask==1.3.0   # Plugin de pytest para Flask